/-!
LeanUI support library.

Currently empty. This is where the Milestone-3 InfoTree / `CodeWithInfos`
extractor meta-programs will live (Lean expression → structured JSON for the
WYSIWYG math editor). For Milestone 1 nothing here is needed: the bridge runs
`lean --json` on the user's source directly.
-/
