@test failure
@name "Deep nesting: (((((Bad -> A) -> A) -> A) -> A) -> A) -> Bad is negative"
@error "negative occurrence"

inductive Nat : Type where
  Zero : Nat
  Succ : Nat -> Nat

inductive Bad : Type where
  bad : (((((Bad -> Nat) -> Nat) -> Nat) -> Nat) -> Nat) -> Bad
