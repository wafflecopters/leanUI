@test failure
@name "soundness: refl cannot prove Zero = True across different types"
@import preambles/equality.tt

inductive Nat : Type where
  Zero : Nat
  Succ : Nat -> Nat

inductive Bool : Type where
  True : Bool
  False : Bool

bad : Equal Zero True
bad = refl
