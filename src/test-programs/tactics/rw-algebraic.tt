@test success
@name "rw: algebraic proof pattern (addZeroLeft style)"
@import preambles/nat.tt

inductive Equal : {A : Type} -> A -> A -> Type where
  refl : {A : Type} -> {a : A} -> Equal a a

sym : {A : Type} -> {x y : A} -> Equal x y -> Equal y x
sym refl = refl

trans : {A : Type} -> {x y z : A} -> Equal x y -> Equal y z -> Equal x z
trans refl refl = refl

cong : {A B : Type} -> {x y : A} -> (f : A -> B) -> Equal x y -> Equal (f x) (f y)
cong f refl = refl

replace : {A : Type} -> {x y : A} -> (P : A -> Type) -> Equal x y -> P x -> P y
replace P refl px = px

-- Postulated operations (simulating record projections)
postulate plus : Nat -> Nat -> Nat
postulate plusZeroRight : (a : Nat) -> Equal (plus a Zero) a
postulate plusComm : (a b : Nat) -> Equal (plus a b) (plus b a)

-- addZeroLeft pattern: plus Zero a = a
-- Term mode: trans (plusComm Zero a) (plusZeroRight a)
-- Tactic mode:
addZeroLeft : (a : Nat) -> Equal (plus Zero a) a := by
  intro a
  rw (plusComm Zero a), (plusZeroRight a)
