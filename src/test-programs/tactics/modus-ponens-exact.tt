@test success
@name "modusPonens with direct exact"

-- Tests that implicit type arguments can be introduced and used correctly in tactics
-- Previously had de Bruijn indexing issues, now fixed!
modusPonens : {A B : Type} -> A -> (A -> B) -> B := by
  intros A B a f
  exact (f a)
