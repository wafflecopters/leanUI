@test success
@name "rewrite tactic with simple equality"
@import preambles/nat.tt
@import preambles/equality.tt

-- Test rewrite on a simple case
-- Given h : n = m, rewrite h changes goal from n = m to m = m
testRewrite : (n m : Nat) -> Equal n m -> Equal n m := by
  intro n m h
  exact h
