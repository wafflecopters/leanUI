@test success
@name "rewrite: basic equality rewrite"
@import preambles/nat.tt

inductive Equal : {A : Type} -> A -> A -> Type where
  refl : {A : Type} -> {a : A} -> Equal a a

sym : {A : Type} -> {x y : A} -> Equal x y -> Equal y x
sym refl = refl

replace : {A : Type} -> {x y : A} -> (P : A -> Type) -> Equal x y -> P x -> P y
replace P refl px = px

-- rewrite h where h : Equal n m transforms goal from using n to using m
test : {n m : Nat} -> Equal n m -> Equal (Succ n) (Succ m) := by
  intros n m h
  rewrite h
  exact refl
